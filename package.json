{
  "name": "electron-clash-pro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "git submodule init && git submodule update && pnpm i && cd clash-dashboard && pnpm i",
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "start": "npm run build && electron ./dist/main.js",
    "dev:web": "cd clash-dashboard && npm start",
    "build:web": "cd clash-dashboard && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/7zip-min": "^1.3.1",
    "electron": "^16.0.4",
    "tslib": "^2.3.1",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "7zip-min": "^1.4.1",
    "got": "^11.8.3",
    "proxy-agent": "^5.0.0"
  }
}
